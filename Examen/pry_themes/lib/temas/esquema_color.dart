import 'package:flutter/material.dart';

// TEMA: Paleta de Colores
// Define los colores base de la aplicación
// Colores: azul (primario), gris (secundario), fondo claro

class ColoresApp {
  // Color primario: azul (AppBar, botones principales)
  static const Color primario = Color(0xFF1565C0);
  // Color secundario: gris (elementos secundarios)
  static const Color secundario = Color(0xFF90A4AE);
  // Colores de estado
  static const Color acento = Color(0xFF7C4DFF);
  static const Color exito = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
  // Fondo claro
  static const Color fondo = Color(0xFFF5F7FA);
  // Textos
  static const Color textoOscuro = Color(0xFF212121);
  static const Color textoClaro = Color(
    0xFFFFFFFF,
  ); // Blanco para contraste en fondos oscuros
}
